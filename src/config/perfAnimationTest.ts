/**
 * 성능 비교용 — 끝나면 둘 다 `false` 로 되돌리기.
 *
 * - `DISABLE_GLTF_ANIMATIONS`: world / CH / 분리 GLB의 `useAnimations` `.play()` 생략
 * - `DISABLE_PROCEDURAL_MAP_ANIMATIONS`: 연기·물결·네온 스크롤 등 맵 장식 `useFrame` 생략
 */
export const DISABLE_GLTF_ANIMATIONS = true
export const DISABLE_PROCEDURAL_MAP_ANIMATIONS = true
